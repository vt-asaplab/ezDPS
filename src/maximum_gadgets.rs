use libspartan::{Instance, VarsAssignment, InputsAssignment};
use curve25519_dalek::scalar::Scalar;


/// This function is one of the building blocks of the machine learning pipeline.
/// The goal of this function is to prove the first element in an array is the maximum value among
/// the array. The variables are described as
/// The constraints can be described as

pub fn maximum_gadgets() -> (
    // usize,
    // usize,
    // usize,
    // usize,
    // Instance,
    // VarsAssignment,
    // InputsAssignment,
) {
    let test = 1;
    // let test2 = test.into_bits();

}